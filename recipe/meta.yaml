{% set name = "py3dep" %}
{% set version = "0.1.7" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 848186af99e0f50fca3a5622688cc398feef0b6e64c3f373139b7dfba873123b

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . --no-deps -vv

requirements:
  host:
    - python >=3.6
    - pip
    - setuptools_scm
  run:
    - python >=3.6
    - pygeoogc
    - pygeoutils
    - xarray
    - scipy
    - rasterio
    - shapely
    - dataclasses

test:
  imports:
    - py3dep

about:
  home: https://github.com/cheginit/py3dep
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: Access to 3DEP database for getting data such as DEM data within the US.

  description: |
    Py3DEP is a part Hydrodata software stack that handles accessing to the
    National Map's 3D Eelevation Program for getting data such as DEM, slope,
    and aspect.
  doc_url: https://hydrodata.readthedocs.io
  dev_url: https://github.com/cheginit/py3dep

extra:
  recipe-maintainers:
    - cheginit
